# 중복 처리 로직 테스트 시나리오

## 이전 문제점들
1. **연속 메시지 차단**: 사용자가 "ㅋㅋㅋ"를 연속으로 보내면 두 번째부터 차단됨
2. **알림 누락**: 같은 사용자가 5초 내에 보낸 메시지들의 알림이 모두 차단됨  
3. **재시도 메시지 차단**: 네트워크 재시도로 다시 전송된 정상 메시지가 차단됨

## 수정된 중복 방지 로직

### Service Worker (sw.js)
- **이전**: 같은 사용자 + 같은 텍스트 + 3초 이내 = 중복 차단
- **수정 후**: 정확한 ID 일치 + 0.5초 이내만 중복 차단
- **알림**: 사용자별 5초 → 메시지별 2초 단위 중복 방지

### MessageBuffer (messageBuffer.ts)  
- **이전**: 같은 사용자 + 같은 텍스트 + 3초 이내 = 중복 차단
- **수정 후**: 정확한 ID 일치 + 1초 이내만 중복 차단

### Pusher Hook (usePusher.ts)
- **이전**: 메시지 ID로 10초간 알림 차단  
- **수정 후**: 메시지 ID + 3초 윈도우로 더 유연한 차단

## 테스트해야 할 시나리오들

### ✅ 정상 작동해야 하는 경우들:
1. **연속 동일 메시지**: 
   - 사용자 A가 "안녕" 전송 → 즉시 "안녕" 다시 전송
   - 둘 다 표시되어야 함

2. **빠른 연속 메시지**:
   - 사용자 A가 "ㅋㅋㅋ", "ㅋㅋㅋ", "ㅋㅋㅋ" 1초 간격으로 전송
   - 모두 표시되어야 함

3. **여러 사용자 동일 메시지**:
   - 사용자 A가 "좋아요" → 사용자 B가 "좋아요"
   - 둘 다 표시되어야 함

4. **백그라운드 복귀 시**:
   - 앱이 백그라운드에 있을 때 여러 메시지 수신
   - 복귀 시 모든 메시지가 순서대로 표시되어야 함

### ❌ 여전히 차단되어야 하는 경우들:
1. **진짜 중복**:
   - 네트워크 오류로 0.5초 내에 완전히 동일한 메시지 재전송
   - 한 번만 표시되어야 함

2. **스팸성 알림**:
   - 같은 메시지에 대해 3초 내 반복 알림  
   - 첫 번째 알림만 표시되어야 함

## 실제 테스트 방법
1. 개발자 도구 콘솔에서 로그 확인
2. 두 개의 브라우저/탭으로 서로 다른 사용자로 테스트
3. 백그라운드 상태에서 메시지 수신 테스트
4. 네트워크 throttling으로 재시도 상황 테스트

## 모니터링 포인트
- 콘솔에서 "중복 메시지 스킵" 로그 빈도 확인  
- "Recent notification exists" 로그 패턴 확인
- 실제 사용자 피드백으로 메시지/알림 누락 여부 확인